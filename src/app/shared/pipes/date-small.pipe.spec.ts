import { DateSmallPipe } from './date-small.pipe';

describe('DateSmallPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSmallPipe();
    expect(pipe).toBeTruthy();
  });
});
